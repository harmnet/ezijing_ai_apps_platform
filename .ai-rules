# 紫荆AI应用平台项目指南

## 项目架构

本项目是一个基于Vue 3和Python的AI应用平台，包含以下主要组件：

- 前端：Vue 3单页应用，位于frontend目录
- 后端：Python服务，位于backend目录
- 配置文件：位于根目录

## 代码规范

### 通用规范

1. 所有代码必须有清晰的注释，特别是复杂的算法和业务逻辑
2. 重构时保留原始代码的核心功能，不随意更改API接口
3. 严格控制第三方依赖的引入
4. 所有用户界面文本必须使用中文
5. **所有服务的重启或启动必须使用restart.sh脚本，不允许直接启动服务**
6. **排查问题时禁止创建新的虚拟环境，必须使用项目现有环境**
7. **编写代码或排查问题时必须使用真实数据，禁止使用模拟数据**

### 前端规范

1. 严格使用Vue 3的组合式API，不使用选项式API
2. 使用直接赋值更新数组元素，不使用Vue 2的$set方法
3. 所有组件命名使用PascalCase
4. 保持CSS类名的一致性，使用kebab-case
5. 优先使用CSS变量统一主题色
6. **严格禁止使用任何Vue 2的代码逻辑和API，确保与Vue 3兼容**
7. **所有测试文件必须放置在frontend/test目录中**

### 后端规范

1. 遵循RESTful API设计原则
2. 所有API返回统一格式：{"status": "success|error", "data": {...}}
3. 使用专用的流式响应API处理流式请求
4. 日志记录必须包含足够的上下文信息
5. 敏感配置通过环境变量管理
6. **所有测试文件必须放置在backend/test目录中**

## 开发流程

1. 理解现有代码结构和设计原则
2. 先编写测试，再实现功能
3. 验证所有错误处理场景
4. 确保流式API的兼容性和稳定性
5. **使用restart.sh脚本启动或重启服务进行测试，不直接启动服务**

## 流式响应处理准则

1. 前端必须正确处理SSE格式的流式响应
2. 请求头必须包含`Accept: text/event-stream`
3. 后端响应头必须包含`Content-Type: text/event-stream`
4. 流式数据格式必须遵循`data: {...}\n\n`的格式

## 安全规范

1. 所有用户输入必须验证和清理
2. 不在代码中硬编码任何密钥或敏感信息
3. 前端不直接处理敏感业务逻辑

## 测试规范

1. **前端测试文件必须放在frontend/test目录中**
2. **后端测试文件必须放在backend/test目录中**
3. **所有测试必须使用真实数据结构，不使用假数据**
4. **测试代码也必须遵循代码规范和命名约定**
5. **测试执行必须在项目现有环境中进行，不创建新的虚拟环境**

请在编写或修改代码时遵循以上规则。 